name: delete-old-packages
on:
  workflow_dispatch:

jobs:
  delete-old-packages:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 1
      matrix: 
        node: ['sample']
      fail-fast: false 
    
    steps:
      - name: Delete package
        uses: actions/delete-package-versions@56849b848a115d2554824e0d56218f3324c2541a
        with: 
            owner: NamrataJha
            repo: 'sample'
            package-name: ${{ matrix.node }}
            min-versions-to-keep: 100


 


          
